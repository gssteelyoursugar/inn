<!--pages/order/tabs/index.wxml-->

<view class="order-tabs">
  <view class="tab-item" wx:for="{{tabs}}" wx:key="{{item.id}}" catchtap="clickTab" data-index="{{index}}">
    <text class="{{index === curIndex? 'active':''}}">{{item.name}}</text>
  </view>
</view>